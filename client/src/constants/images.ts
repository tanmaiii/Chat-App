import google from "../assets/images/google.png"; // Ensure this path is correct and the file exists
import github from "../assets/images/github.png"; // Ensure this path is correct and the file exists

const Imgaes = {
  GOOGLE: google,
  GITHUB: github,
};

export default Imgaes;
